services:
  rssfeed:
    image: ${DOCKER_IMAGE:-thititongumpun/elysia-rssfeed}:${CONTAINER_VERSION:-latest}
    container_name: rssfeed
    ports:
      - "3001:3000"
    restart: always
    env_file:
      - path: .env
    environment:
      - TZ=Asia/Bangkok